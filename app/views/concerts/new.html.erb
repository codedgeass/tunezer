<p> <b> Search for an Artist or Festival </b> </p>
<%= form_tag new_concert_path, method: :get do %>
    Find a Concert: <%= text_field_tag 'new_search' %>
<% end %>
<div id="new_rankings"></div>
<p> <b> Or create a new one </b> </p>
<%= form_for @concert do |f| %>
	<%= render 'shared/error_messages', target: @concert %>
	<div class="field">
    <%= f.label(:name, "Event or Performer's Name:") %>
	  <%= f.text_field :name %> 
  </div>
  <div class="field">
    <%= f.label :street_address %>
    <%= f.text_field :street_address %>
  </div>
  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <%= f.label :zip %>
    <%= f.text_field :zip %>
  </div>
  <div class="field">
    <%= f.label :country %>
    <%= f.text_field :country %>
  </div>
	<% genres = 
       ['Blues', 'Country', 'Classical', 'Electronic', 'Ethnic', 'Hip Hop', 'Jazz', 'Pop', 'R&B', 'Rock', 'Other'] %>
	<div class="field"> Genre: <%= f.select(:genre, genres, { include_blank: true }) %> </div>
	<div class="actions"> <%= f.submit 'Create Event' %> </div>
<% end %>