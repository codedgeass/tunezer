<%= form_for [@production, @comment], remote: true do |f| %>
	<%= render 'shared/error_messages', target: @comment %>
  <%= f.text_area :content %>
	<%= f.hidden_field :username, { value: current_or_guest_user.username } %>
  <br>
  <%= f.submit %>
<% end %>